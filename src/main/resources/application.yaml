spring:
  application:
    name: sams
  datasource:
    url: jdbc:mysql://MF457aXt2XdNUGx.root:S1aiY5zROXBrgZC7@gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/sams?sslMode=VERIFY_IDENTITY
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  Jackson:
    serialization:
      indent-output: true

  servlet:
    multipart:
      max-file-size: 50MB
logging:
  level:
    root: info
    org.springframework: INFO
    org.hibernate.sql: debug

server:
  port: 8080
  servlet:
    context-path: /api

springdoc:
  api-docs:
    path: /docs
  swagger-ui:
    path: /swagger